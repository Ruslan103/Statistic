meta:
  environment: jvm
  toolchain:
    name: maven
    version: 11
build:
  artifacts:
    target/*.jar: /
run:
  jarName: target/statistic-0.0.1-SNAPSHOT.jar.original
  persistenceMount: /data
  containerPort: 80
