meta:
  environment: jvm
  toolchain:
    name: maven
    version: 11
build:
  artifacts:
    target/*.jar: /
run:
  jarName: .\statistic\target\statistic-0.0.1-SNAPSHOT.jar
  persistenceMount: /data
  containerPort: 80
